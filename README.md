Returns the nickname of a given U.S. state.



@param State (string) — the name of the U.S. state entered by the user
@return string — returns the state nickname, 
"incorrect input"
 if empty, or 
"Not found"
 if not found
function getStateNickname(State)


Returns the population of a given U.S. state.



@param State (string) — the name of the U.S. state entered by the user
@return string/number — returns the state population, 
"incorrect input"
 if empty, or 
"Not found"
 if not found
function getStatePopulation(State)


Returns the capital city of a given U.S. state.



@param State (string) — the name of the U.S. state entered by the user
@return string — returns the capital city, 
"incorrect input"
 if empty, or 
"Not found"
 if not found
function getStateCapital(State)

description of what your function does
Returns the median household income of a given U.S. state.



@param State (string) — the name of the U.S. state entered by the user
@return string/number — returns the median income, 
"incorrect input"
 if empty, or 
"Not found"
 if not found
function getStateMedianIncome(State)

Returns the percentage of people living in poverty in a given U.S. state.



@param State (string) — the name of the U.S. state entered by the user
@return string/number — returns the poverty percentage, 
"incorrect input"
 if empty, or 
"Not found"
 if not found
function getStatePovertyPercent(State)
